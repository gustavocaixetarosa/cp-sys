# PostgreSQL Configuration
POSTGRES_DB=cobranca
POSTGRES_USER=gustavo
POSTGRES_PASSWORD=139150

# Backend API URL (usado pelo frontend)
# Para desenvolvimento local, use: http://localhost:8080
# Para VPS, use o IP ou domínio da sua VPS: http://seu-ip:8080 ou http://api.seu-dominio.com
VITE_API_URL=http://localhost:8080

# Spring Boot Configuration (opcional, já configurado no docker-compose)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

