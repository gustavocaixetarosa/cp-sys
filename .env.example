# ================================
# CP-SYS Environment Configuration
# ================================

# Database Configuration
POSTGRES_DB=cobranca
POSTGRES_USER=gustavo
POSTGRES_PASSWORD=your_secure_password_here

# Spring/Backend Configuration
SPRING_JPA_HIBERNATE_DDL_AUTO=update
# Options: none, validate, update, create, create-drop

# Application URLs
APP_FRONTEND_URL=http://localhost:5173
# Production: https://yourdomain.com

VITE_API_URL=http://localhost:8080
# Production: https://api.yourdomain.com

# JWT Configuration (opcional)
JWT_SECRET=your_jwt_secret_key_here_at_least_32_chars

# Server Configuration (opcional)
SERVER_PORT=8080

# Database Connection Pool (opcional)
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=10
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=5

# Logging Configuration (opcional)
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_DEV_GUSTAVOROSA_COBRANCA_CP=DEBUG

# ================================
# VPS/Production Only
# ================================

# SSH Configuration (for GitHub Actions)
# VPS_HOST=your.vps.ip.address
# VPS_USER=your_ssh_user
# VPS_PORT=22
# VPS_SSH_KEY=your_private_ssh_key

# Notification Webhooks (opcional)
# DISCORD_WEBHOOK=https://discord.com/api/webhooks/...
# SLACK_WEBHOOK=https://hooks.slack.com/services/...

# ================================
# Notes
# ================================
# 1. Never commit the actual .env file to git
# 2. Copy this file to .env and fill in your values
# 3. Sensitive values (passwords, secrets) should be stored in GitHub Secrets
# 4. Use strong, unique passwords for production
